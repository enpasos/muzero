package ai.enpasos.muzero.platform.config;

public enum RunType {
    NONE, BADACTION, INMIND, TEST, TRAIN, ACTIONS, STARTVALUES, VALUE, VALUES, LOSS, RENDER, ONNX, INFERENCE, ELO, SURPRISE, SURPRISEEXTRACT, ENTROPY, POLICYONLY
}
