package ai.enpasos.muzero.platform.config;

public enum RunType {
    NONE, TEST, TRAIN, TRAIN2,STARTVALUES, VALUE, VALUES, LOSS, RENDER, WINLOOSE, ONNX, INFERENCE, VALUESELFCONSISTENCY, ELO, SURPRISE, ENTROPY, POLICYONLY
}
